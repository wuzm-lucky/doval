# 有钱记前端开发日志

## 2026-09-05｜M1/M5｜前端｜模块接入与资产概览

- 计划目标：建立可独立启用的有钱记前端模块，并交付第一个真实消费后端数据的资产概览页面。
- 已完成：新增 `src/modules/youqianji/` 模块，提供集中 API 封装、有钱记路由与资产概览页面。页面支持输入并本地记住家庭 ID、选择估值币种，调用家庭资产和可见账户接口，展示资产、负债、净资产、账户属性和历史汇率质量提示；未在前端推算金额或汇率。
- 修改范围：有钱记模块 API、路由、概览页面和模块说明。
- 联调接口：`GET /youqianji/report/asset`、`GET /youqianji/account/list/{familyId}`。
- 验证：使用 `npm run build -- --modules=base,youqianji` 构建成功。首次构建发现既有验证码组件声明的 `crypto-js` 未安装，执行 `npm install` 补齐后通过。
- 风险与遗留：当前路由须使用 `--modules=base,youqianji` 启用；尚未在已登录真实账号和家庭样本下完成接口交互验证。账户、流水、快照、应收应付、计划和报表完整页面仍待开发。
- 下一步：实现账户与流水页面，并接入家庭上下文和后端流水受控查询。

## 2026-09-05｜M1/M2｜前端｜账户与流水查询

- 计划目标：交付 P0 的可见账户和已记账流水查询页面。
- 已完成：新增账户页与流水页路由。账户页仅调用后端可见账户列表并展示资产/负债、Ledger/Snapshot、可见性、报表策略和状态；流水页按家庭和日期范围调用后端受控流水列表，不在前端过滤私有账户或计算资金结果。
- 修改范围：有钱记路由、账户页面、流水页面。
- 联调接口：`GET /youqianji/account/list/{familyId}`、`GET /youqianji/transaction/list`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：页面暂以用户输入的家庭 ID 作为工作上下文；尚未连接真实登录家庭数据。流水详情、冲正、资金录入、快照、应收应付、计划与报表完整页面仍待开发。
- 下一步：实现流水详情和资金变更录入页面，继续接入后端写入接口。

## 2026-09-05｜M2｜前端｜同币种账户划转录入

- 计划目标：提供不在前端拼装账务分录的安全账户划转入口。
- 已完成：新增资金变更路由和账户划转表单。表单收集家庭、源/目标账户、币种、原币金额、资金归属、发生时间和备注；前端仅阻止同账户划转及重复提交，实际借贷平衡、权限、归属与 Snapshot Bridge 规则全部交由 `POST /youqianji/transaction/transfer` 服务端处理。成功后返回流水列表。
- 修改范围：有钱记 API、路由、资金变更页面。
- 联调接口：`POST /youqianji/transaction/transfer`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：资金归属主体暂以 ID 输入，待家庭主体受控查询接口完善后替换为选择器；换汇、退款、冲正和通用收入/支出表单尚待开发。
- 下一步：实现流水详情与冲正入口，并继续接入换汇和退款页面。

## 2026-09-05｜M2｜前端｜流水详情与冲正

- 计划目标：让已记账流水保持只读，并提供可追溯的冲正入口。
- 已完成：新增流水详情路由，从流水列表跳转后查询后端受控详情；页面只展示状态、类型、时间、记账币种和备注，不提供编辑或删除。冲正操作具有确认对话框和提交中状态，调用 `POST /youqianji/transaction/reversal` 后回到列表。
- 修改范围：有钱记 API、路由、流水列表和流水详情页面。
- 联调接口：`GET /youqianji/transaction/get/{id}`、`POST /youqianji/transaction/reversal`。
- 验证：修正列表操作列中不符合普通脚本块的 JSX 后，`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：详情尚未展示后端分录与归属明细；换汇、退款和通用收入/支出录入页面仍待开发。
- 下一步：实现换汇和退款录入页面，并补齐快照与报表视图。

## 2026-09-05｜M2｜前端｜资金归属主体选择

- 计划目标：移除资金划转页面中手工输入内部主体主键的过渡实现。
- 已完成：划转表单输入家庭 ID 后调用家庭主体列表接口，将资金归属改为可筛选下拉选择；主体仍由后端按当前成员权限过滤。
- 修改范围：有钱记 API、账户划转页面。
- 联调接口：`GET /youqianji/subject/list/{familyId}`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：账户仍以 ID 输入，待账户选择器组件开发后替换；换汇、退款、快照与报表页面仍待开发。
- 下一步：实现换汇与退款录入页面。

## 2026-09-05｜M2｜前端｜换汇与退款录入

- 计划目标：继续覆盖后端已交付的跨币种换汇和支出退款业务入口。
- 已完成：新增跨币种换汇和支出退款路由及表单。换汇页面收集两端账户、原币金额与资金归属，退款页面绑定原支出流水和到账账户；均只提交业务意图，成交汇率、退款上限、分类继承、分录与收支抵减全部由后端处理。
- 修改范围：有钱记 API、路由、换汇页面、退款页面。
- 联调接口：`POST /youqianji/transaction/exchange`、`POST /youqianji/transaction/refund`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：换汇和退款页面的账户、主体暂仍部分使用 ID 输入；快照、应收应付、计划与报表完整页面仍待开发。
- 下一步：实现快照查询与录入页面。

## 2026-09-05｜M3/M5｜前端｜快照历史与资产报表

- 计划目标：为 Snapshot 账户和资产估值提供只读追溯视图。
- 已完成：新增账户快照历史页，按后端可见账户查询版本化快照；新增家庭资产报表页，展示服务端计算的资产、负债和净资产。页面明确不支持直接编辑快照或在前端推算估值。
- 修改范围：有钱记 API、路由、快照页面、资产报表页面。
- 联调接口：`GET /youqianji/snapshot/list`、`GET /youqianji/report/asset`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：快照录入、趋势/归属/收支等完整报表、应收应付和计划页面仍待开发；尚未使用真实登录家庭样本完成浏览器端到端验证。
- 下一步：实现应收应付和计划列表页，继续补齐 M4 页面。

## 2026-09-05｜M4｜前端｜应收应付与计划状态视图

- 计划目标：展示后端重算的应收应付和计划状态，避免前端维护累计金额或逾期判断。
- 已完成：新增应收应付列表和收支计划页面。应收应付页展示方向、本金、已结算、到期时间和状态；计划页以标签页展示模板与周期实例，并展示计划/完成金额和状态。
- 修改范围：有钱记 API、路由、应收应付页面、计划页面。
- 联调接口：`GET /youqianji/receivable-payable/list/{familyId}`、`GET /youqianji/plan/template/list/{familyId}`、`GET /youqianji/plan/instance/list/{familyId}`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：应收应付创建、收还款、计划创建和实际匹配表单尚待开发；未进行真实登录浏览器联调。
- 下一步：实现快照录入与 M4 写入表单，随后完善报表筛选和端到端验证。

## 2026-09-05｜M3｜前端｜快照录入

- 计划目标：提供最小快照录入闭环，并保留后端对版本、账户模式和余额归属的最终校验。
- 已完成：快照历史页新增录入对话框，可提交单币种余额与对应主体归属；提交成功后刷新该账户的快照历史。页面提示快照更正会由服务端形成新版本，不能直接编辑历史快照。
- 修改范围：快照页面。
- 联调接口：`POST /youqianji/snapshot/post`、`GET /youqianji/snapshot/list`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：多币种、多主体归属拆分与外部集成同步页面尚待开发；真实登录数据下的提交联调未执行。
- 下一步：实现应收应付创建和收还款表单，并补充报表筛选。

## 2026-09-05｜M4｜前端｜借入借出原子记账

- 计划目标：为借入与借出提供仅提交业务意图的前端入口。
- 已完成：新增借入/借出页面，提交家庭、方向、对方主体、资金账户和本金；后端原子完成应收应付、内部记账科目和正式流水创建。
- 修改范围：有钱记 API、路由、借入借出页面。
- 联调接口：`POST /youqianji/receivable-payable/loan`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：收款/还款、计划写入和真实登录联调仍待完成。
- 下一步：实现收款/还款原子结算表单。

## 2026-09-05｜M4｜前端｜收还款与计划写入

- 计划目标：补齐应收应付结算及计划模板的前端写入入口。
- 已完成：新增收款/还款结算页，提交本金、利息、费用和资金账户至后端原子结算接口；新增计划模板创建页，提交收入/支出类型、主体、金额、周期和开始日期。
- 修改范围：有钱记 API、路由、收还款页面、计划创建页面。
- 联调接口：`POST /youqianji/receivable-payable/settle-post`、`POST /youqianji/plan/template/create`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：计划实例创建、实际匹配、收还款快捷入口和真实登录端到端验证仍待完成；部分页面仍使用 ID 输入，待领域选择器完善。
- 下一步：执行后端与前端全量构建回归，梳理 M6 剩余端到端验证项。

## 2026-09-05｜M4｜前端｜计划周期创建

- 计划目标：让计划模板能够落到明确的业务周期，供后端匹配实际流水和重算状态。
- 已完成：新增计划周期创建页，提交模板、周期开始/结束日期和可选截止时间。
- 修改范围：有钱记 API、路由、计划周期创建页面。
- 联调接口：`POST /youqianji/plan/instance/create`。
- 验证：`npm run build -- --modules=base,youqianji` 成功。
- 风险与遗留：计划实际流水匹配页面、真实登录端到端联调、权限回归和窄屏视觉验收仍未完成。
- 下一步：审计前后端接口覆盖度并补齐缺失页面或接口。
## 2026-09-05（M4 页面入口与 M6 代码质量）

- 在“应收应付”列表页增加“新增借贷”“登记结算”入口；在“收支计划”页增加“新增模板”“生成实例”“关联流水”入口，已完成的写入能力不再依赖手工输入路由地址。
- 对 src/modules/youqianji 全量执行 Prettier 格式化；后续将使用模块级格式检查和构建作为基础验收。
- 已通过模块级 Prettier 复验及 npm run build -- --modules=base,youqianji 生产构建；构建仅保留既有主包体积提示，未出现有钱记模块编译错误。

## 2026-09-05｜M5｜前端｜全量报表对接

- 计划目标：将后端已提供的资产、收支、应收应付、资金归属和资产趋势报表统一接入前端，避免报表页只展示资产摘要。
- 已完成：扩展有钱记 API 封装，新增收入支出、应收应付、资金归属接口；报表页新增统一家庭、币种、日期筛选，并通过五个页签展示资产、收支、应收应付、资金归属与资产趋势。资金归属保留原币余额和估值质量，趋势复用后端日维度口径。
- 修改范围：src/modules/youqianji/api/index.js、src/modules/youqianji/pages/report/index.vue、前端开发日志。
- 联调接口：GET /youqianji/report/asset、/income-expense、/receivable-payable、/ownership、/asset-trend。
- 验证：有钱记模块 Prettier 格式检查通过；npm run build -- --modules=base,youqianji 成功。
- 风险与遗留：尚未在真实登录家庭数据下验证五类报表的返回字段、空态及异常提示；前端主包体积提示仍待按实际访问数据评估拆包。
- 下一步：取得隔离测试账号与数据后执行报表页登录态端到端联调，并记录接口与视觉验收结果。

## 2026-09-05｜M3｜前端｜多币种快照与归属拆分

- 计划目标：消除快照页仅支持单币种、单主体且依赖手工输入 ID 的限制。
- 已完成：快照页按家庭加载可见账户和主体并使用可搜索选择器；录入弹窗支持新增/删除多个币种余额，以及每个余额下多个主体的归属拆分和变动说明。提交时保留后端要求的余额与归属金额结构。
- 修改范围：src/modules/youqianji/pages/snapshot/index.vue、前端开发日志。
- 联调接口：GET /youqianji/account/list/{familyId}、GET /youqianji/subject/list/{familyId}、GET /youqianji/snapshot/list、POST /youqianji/snapshot/post。
- 验证：`npm run build -- --modules=base,youqianji` 成功；构建仅保留既有主包体积提示。
- 风险与遗留：需要使用真实登录态确认账户可见性筛选、空数据提示和多主体拆分金额不平衡时的后端错误展示。

## 2026-09-05｜M2｜前端｜流水服务端分页

- 已完成：流水页改用 `GET /youqianji/transaction/pagelist`，接收后端页码、每页数量和总数，并通过表格分页组件请求下一页；筛选条件变化时自动回到第一页。
- 修改范围：src/modules/youqianji/api/index.js、src/modules/youqianji/pages/transaction/index.vue、前端开发日志。
- 验证：`npm run build -- --modules=base,youqianji` 成功；仅保留既有主包体积提示。

## 2026-09-05｜M2｜前端｜账户划转领域选择器

- 已完成：账户划转页的划出与转入账户由原始 ID 输入改为根据家庭加载的可搜索账户选择器；保留主体选择、前端同账户拦截和后端权限校验。
- 修改范围：src/modules/youqianji/pages/transaction/create.vue、前端开发日志。
- 验证：`npm run build -- --modules=base,youqianji` 成功；仅保留既有主包体积提示。

## 2026-09-05｜M2｜前端｜收入支出正式记账

- 已完成：新增收入/支出通用记账页；页面按家庭拉取可见账户、受控账户科目映射、可见分类和主体，根据收支类型自动生成借贷分录，并使用一次性 `sourceKey` 防止网络重试产生重复流水。
- 修改范围：src/modules/youqianji/api/index.js、src/modules/youqianji/pages/transaction/post.vue、路由、流水入口、前端开发日志。
- 联调接口：GET /youqianji/posting-subject/account/list/{familyId}、GET /youqianji/category/list/{familyId}、POST /youqianji/transaction/post。
- 验证：`npm run build -- --modules=base,youqianji` 成功；仅保留既有主包体积提示。
