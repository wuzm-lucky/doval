# Doval 前端模块化迁移方案

## 1. 背景与目标

当前项目是基于 Vue 3、Vite、Vue Router、Pinia 的单页面应用。后端采用模块化能力，并根据已启用模块返回动态菜单。本次改造的目标是在保持单页面应用和单一构建产物的前提下，将前端代码划分为框架模块与可选业务模块。

改造完成后，应用支持以下命令：

```bash
npm run dev -- --modules=base,web3
npm run build -- --modules=base,web3
```

其中 `framework` 始终启用且不需要传入参数；未传 `--modules` 时默认启用 `base`。一次启动或构建只包含 `framework` 和本次指定的业务模块。

## 2. 已确认的架构约定

### 2.1 模块层级和依赖方向

```text
业务模块（base、web3、...）
            │
            ▼
framework（框架模块）
```

- `framework` 是唯一基础依赖层，不能导入任何业务模块。
- 每个业务模块可以导入自身文件、`framework` 提供的公开能力，以及其他业务模块中的实际文件。
- 业务模块启用范围完全由人工传入的 `--modules` 参数决定，不在模块内声明依赖、不自动补齐依赖，也不进行构建期依赖校验。
- 需要被其他业务模块复用的能力，可直接通过其实际文件路径引用。
- 模块名必须与 `src/modules/<模块名>` 的目录名完全一致，且大小写敏感。

### 2.2 framework 的职责

以下现有能力全部归属 `framework`：

- 应用入口、应用壳、布局及布局组件。
- 登录、首页、用户中心、结果页和 404 页面。
- 路由实例、路由守卫、权限处理及动态菜单展示。
- Pinia 实例、用户/菜单/通知/设置/页签等全局状态。
- Axios 请求封装、认证/菜单/文件等框架级接口。
- 国际化实例、语言切换、全局样式、主题、配置、通用工具、通用组件和静态资源。

后端负责返回当前启用模块对应的菜单。前端仅展示后端菜单；菜单路径没有已注册的前端路由时，沿用现有兜底路由跳转至 `/result/404`，不增加菜单过滤或额外校验逻辑。

### 2.3 业务模块自治范围

每个业务模块在自身目录下管理以下内容：

```text
src/modules/<模块名>/
├── api/                    # 仅本模块接口
├── assets/                 # 仅本模块静态资源
├── components/             # 仅本模块组件
├── locales/                # 仅本模块语言包
├── pages/                  # 仅本模块页面
├── router/                 # 仅本模块路由声明
├── store/                  # 仅本模块 Pinia Store
├── utils/                  # 仅本模块工具
└── bootstrap.js            # 可选：模块初始化
```

业务模块可以按需要省略无内容的目录。模块目录名即模块名称，不需要 `index.js`、`module.config.js`、`dependencies` 或其他公开声明文件。模块间需要复用代码时，可直接引用目标模块中的实际文件路径。

### 2.4 模块注册契约

框架根据已启用模块目录，按固定目录约定聚合路由、国际化资源和可选初始化文件；路由、状态和国际化实例本身仍由 `framework` 统一创建和维护。

推荐契约如下：

```js
export function install(app, context) {
  // 可选：注册模块全局组件或执行模块初始化。
}
```

- 路由文件放入 `router/`，由框架合并到唯一的 Router 实例。
- 语言包放入 `locales/`，文案必须使用模块命名空间，例如 `web3.wallet.connect`，避免语言键冲突。
- `bootstrap.js` 为可选文件，导出 `install(app, context)` 函数；在框架完成 Router、Pinia、i18n 初始化后调用。
- 模块全局组件必须使用模块前缀命名，例如 `web3-wallet-widget`。框架通过 `context.registerGlobalComponent` 注册，并在组件名重复时直接失败，禁止静默覆盖。
- `framework` 始终最先初始化；多个业务模块的 `bootstrap.js` 按 `--modules` 参数中的书写顺序执行。

跨模块复用规则：

- 业务模块可直接引用其他模块中所需的实际文件路径，例如 `@modules/base/components/UserSelector.vue`。
- 需要被多个模块普遍使用且不属于业务语义的能力，仍应沉淀到 `framework`。

### 2.5 构建参数与错误处理

不新增 Node 启动包装脚本。继续使用 Vite 的 npm 脚本，并在 `vite.config.js` 读取 `process.argv` 中的 `--modules=<模块列表>` 参数。

参数规则：

- 未传参数：启用 `base`。
- 传入多个模块：使用英文逗号分隔，例如 `--modules=base,web3`。
- 空模块名、重复模块名、未知模块名均为错误，直接终止启动或构建。
- 未知模块错误日志使用 `Error: module <模块名> not exists`，并保留 Vite 的异常堆栈，效果与既有项目的模块校验保持一致。
- 成功解析时输出本次启用的模块名称和各模块入口，便于核对启动或构建范围。

未启用模块不会注册其路由、语言包和 `bootstrap.js` 中的全局组件或初始化逻辑。若运行时出现因遗漏模块导致的组件不可用问题，由人工使用包含所需模块的命令重新构建；不增加自动依赖补齐或构建期依赖校验。

注意：若模块 A 使用普通 ES `import` 直接导入模块 B 的源码，Vite 会将 B 作为 A 的传递依赖纳入产物。这种导入仅复用源码，不代表 B 的路由、语言包和初始化逻辑已被启用；后者仍严格由 `--modules` 参数控制。

不能使用运行时筛选 `import.meta.glob(..., { eager: true })` 来实现模块开关，因为它会将未启用模块也纳入构建分析。应在 Vite 配置加载阶段生成仅含启用模块的虚拟模块清单，使 Rollup 仅静态导入本次选中的模块入口。

## 3. 目标目录结构

```text
src/
├── framework/
│   ├── api/
│   ├── assets/
│   ├── bootstrap/          # 模块解析、注册、应用启动编排
│   ├── components/
│   ├── config/
│   ├── hooks/
│   ├── layouts/
│   ├── locales/
│   ├── router/
│   ├── store/
│   ├── styles/
│   └── utils/
├── modules/
│   ├── base/
│   │   └── bootstrap.js     # 可选：模块初始化
│   └── web3/
│       └── bootstrap.js     # 可选：模块初始化
├── App.vue
└── main.js
```

`App.vue` 和 `main.js` 可暂时保留在 `src` 根目录作为最小应用入口；其业务实现只能依赖 `framework`。如后续需要进一步收敛，也可将其实现迁入 `framework/bootstrap`，但不作为本次迁移的必要条件。

路径别名调整为：

```text
@/                 -> src/
@framework/        -> src/framework/
@<模块名>/          -> src/modules/<模块名>/
```

业务模块内部优先使用相对路径；引用框架公开能力时使用 `@framework/`。模块别名由 Vite 根据 `src/modules` 的一级目录动态生成，跨模块引用直接使用 `@<模块名>/` 下的实际文件路径，例如 `@base/components/UserSelector.vue`。模块目录名不得与保留别名 `@`、`@framework` 冲突。

## 4. 分阶段迁移清单与实施步骤

### 阶段一：建立构建期模块选择机制

1. 在 `vite.config.js` 增加模块参数解析、目录发现、合法性校验和启动日志。
2. 在 Vite 配置中实现虚拟模块清单，仅生成 `framework + 已启用业务模块` 的静态导入语句。
3. 增加 `@framework/` 别名，并根据模块目录动态生成 `@base/`、`@web3/` 等模块别名；同步更新 `jsconfig.json` 中已存在模块的路径提示。
4. 确认以下命令的行为：

   ```bash
   npm run dev
   npm run dev -- --modules=base,web3
   npm run build
   npm run build -- --modules=base,web3
   ```

5. 确认未知模块命令立即失败，例如：

   ```bash
   npm run dev -- --modules=base,cc
   ```

### 阶段二：建立 framework 启动与注册能力

1. 创建 `framework/bootstrap`，负责读取虚拟模块清单、合并模块路由与语言包、调用模块 `install`。
2. 保持全局 Router、Pinia、i18n 实例各自仅创建一次。
3. 提供受控的 `registerGlobalComponent` 注册函数，并校验组件名称重复；模块 `bootstrap.js` 按命令参数顺序执行。
4. 将当前路由自动聚合逻辑替换为“框架固定路由 + 已启用模块声明路由”的聚合方式。
5. 保持现有动态菜单、权限守卫和 404 兜底行为不变。

### 阶段三：迁移现有代码至 framework

按目录移动并修改导入路径：

| 当前目录或文件      | 目标位置                             | 归属说明                       |
| ------------------- | ------------------------------------ | ------------------------------ |
| `src/api/`          | `src/framework/api/`                 | 认证、菜单、文件等框架级接口   |
| `src/assets/`       | `src/framework/assets/`              | 当前均为框架页面或通用组件资源 |
| `src/components/`   | `src/framework/components/`          | 当前通用组件                   |
| `src/config/`       | `src/framework/config/`              | 全局配置                       |
| `src/hooks/`        | `src/framework/hooks/`               | 当前通用组合式函数             |
| `src/layouts/`      | `src/framework/layouts/`             | 应用布局与其组件               |
| `src/locales/`      | `src/framework/locales/`             | 全局 i18n 实例与框架语言包     |
| `src/pages/login/`  | `src/framework/pages/login/`         | 登录属于框架能力               |
| `src/pages/home/`   | `src/framework/pages/home/`          | 首页属于框架能力               |
| `src/pages/user/`   | `src/framework/pages/user/`          | 用户中心属于框架能力           |
| `src/pages/result/` | `src/framework/pages/result/`        | 异常及 404 页面属于框架能力    |
| `src/router/`       | `src/framework/router/`              | 路由实例、守卫和框架固定路由   |
| `src/store/`        | `src/framework/store/`               | 全局 Pinia 与框架状态          |
| `src/style/`        | `src/framework/styles/`              | 全局样式                       |
| `src/utils/`        | `src/framework/utils/`               | 请求、路由、日期等通用工具     |
| `src/permission.js` | `src/framework/router/permission.js` | 框架路由守卫                   |

迁移以小批次进行：每移动一个目录即更新引用，并执行格式检查与启动验证。不得同时重写业务逻辑，确保目录调整与功能变更分离。

### 阶段四：创建默认 base 模块

1. 创建 `src/modules/base/`；目录名 `base` 即模块名称。
2. `base` 初始可不包含路由、语言包或初始化逻辑，但必须是一个可被默认启用的合法模块目录。
3. 后续基础业务功能新增到 `src/modules/base/`，不再放回 `framework`。

### 阶段五：接入 web3 等业务模块

1. 创建 `src/modules/web3/` 的标准自治目录。
2. 将 Web3 专属 API、页面、状态、语言包、资源、工具放入该目录。
3. 通过 `@base/components/...` 直接引用 `base` 模块中的基础业务公共能力时，构建命令必须人工显式指定 `base,web3`。
4. 通过 `bootstrap.js` 声明初始化逻辑；全局 Widget 通过受控注册函数注册。
5. 分别验证仅 `base`、仅 `web3` 与 `base,web3` 三种构建产物和菜单访问行为。

### 阶段六：固化依赖边界与工程文档

1. 为新建模块补充开发说明：目录约定、入口契约、命名空间和构建命令。
2. 更新 `README.md` 的项目结构、启动命令及模块开发章节。

## 5. 验收标准

- `npm run dev` 能启动 `framework + base`。
- `npm run build` 能构建 `framework + base`。
- `npm run dev -- --modules=base,web3` 和对应构建命令只加载指定业务模块。
- 传入不存在、为空或重复的模块名时，命令失败并输出明确错误。
- `framework` 不依赖任何业务模块；实际启用的业务模块完全等于命令行指定的模块集合（未传参时为 `base`）。
- `--modules=base,web3` 按指定顺序初始化 `base` 与 `web3`；遗漏所需模块时，由人工调整命令后重新构建。
- 跨模块复用可直接引用目标模块中的实际文件路径。
- 登录、首页、用户中心、动态菜单、权限守卫、语言切换和主题能力保持原有行为。
- 后端菜单指向未启用模块的页面时，应用展示既有 404 页面。
- 已启用模块的路由、Store、国际化文案及 `install` 初始化均生效。
- 模块全局组件名称冲突时，启动或构建失败，不发生静默覆盖。
- 执行 `npm run format:check` 通过。

## 6. 实施边界与回滚策略

- 本次仅进行前端目录、注册机制与构建选择机制改造，不修改后端菜单接口和后端模块配置逻辑。
- 保持单一应用入口、单一 Router、单一 Pinia、单一 i18n 实例，不引入微前端或多应用运行时。
- 每个阶段完成后均保持可启动、可构建；若某个迁移批次出现回归，只回退该批次的目录移动和导入调整，不影响已验证阶段。
- 构建产物仍输出到现有 `dist/`，部署方式不变。
